import CustomerBrowse from "./CustomerBrowse";

/** @deprecated Use CustomerBrowse — kept for imports that expect UserList */
const UserList = () => <CustomerBrowse initialView="list" />;

export default UserList;
